```
bazel clean && bazel run -- @pnpm//:pnpm --dir $PWD install --lockfile-only && bazel build //:gen
```

https://github.com/protobufjs/protobuf.js/blob/master/CHANGELOG.md?utm_source=chatgpt.com#700-2022-07-08

```
move command line tool to a new package named protobufjs-cli (#1234)
```
